1. Abstract command class, map of params +
2. Main program flow+
3. XML parser+
4. different commands +
5. command factory +
6. load xml at start +
7. handle signals

check commands for final modifier
More info about collection?

execute_script <filepath>input.txt</filepath>
execute_script <filepath>input.txt</filepath>
execute_script <filepath>input.txt</filepath>
execute_script <filepath>input.txt</filepath>
execute_script <filepath>input.txt</filepath>



1. Param +
2. Command +
5. workflow manager
3. Different commands
4. Command factory

//reflection?
abstract command class? and make map for generalization of
    ArrayList<Parameter<?>> getParams();
    ArrayList<Parameter<?>> getMissingParams();
    ArrayList<Parameter<?>> getRemainingRequiredParams();
    void setParam(Parameter<?> param);
    void setParams(ArrayList<Parameter<?>> params);

return copy in get, but don't copy in set

/*

while (true)
readerType
if xml -> xml():
    xml.parse()

console -> console()
    console.parse()

 */

/*
switch
case "1231":



ArrayList params
[aParam, bParam]
aParam:
name = "a"
{
"a" -> showParam
}

 */